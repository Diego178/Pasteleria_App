package com.proyecto.pasteleria.Venta;

import javafx.scene.layout.BorderPane;

public class Venta extends BorderPane {
   /* private Label lTitulo;
    private Stage stage;
    private Button bAgregarPastel;
    private GridPane GPbotones;
    private TablaVentas tablaVentas;
    private ObservableList<Pastel> pasteles;
    public Venta(Stage stage){
        this.stage=stage;
        inicializarComponentes();
    }

    private void inicializarComponentes() {
        GPbotones = new GridPane();
        lTitulo = new Label("Venta");
        tablaVentas=new TablaVentas();
        TableView tablaVentas1=tablaVentas.getTablaPasteles();

        bAgregarPastel= new Button("Agregar pastel");

        setTop(titulo);
        setLeft(tablaVentas1);
    }*/
}
